apiVersion: v1
kind: Service
metadata:
  name: gitops-demo-api-service
  labels:
    app: gitops-demo-api
spec:
  selector:
    app: gitops-demo-api
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  type: ClusterIP